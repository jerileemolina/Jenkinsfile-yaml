data = readYaml file: "release.yaml"
@Library('shared-library') _
pipeline {
    agent any
    stages {
        stage('APP_JAVA-INT') {
        steps {
            echo "APP_JAVA INT... Version..es"
            }
        }
    }
}