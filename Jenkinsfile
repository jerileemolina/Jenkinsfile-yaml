@Library('shared-library') _
pipeline {
    agent any
    yaml'release.yaml'
    stages {
        stage('APP_JAVA-INT') {
            steps {
                llamada()
            }
        }
        stage('APP_JAVA-PRE') {
            steps {
                llamada()
            }
        }
        stage('APP_JAVA-PRE') {
            steps {
                llamada()
            }
        }

    }
}