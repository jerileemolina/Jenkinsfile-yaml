pipeline {
agent
stages {
    stage('primera etapa') {
    steps {
    script {
    while read linea nombre version;do
    echo -e " El nombre es +$nombre y la versión es +$version"
    done
                }
            }
        }
    }
}
