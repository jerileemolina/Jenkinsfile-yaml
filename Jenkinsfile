pipeline {
agent any
stages {
    stage('primera etapa') {
    steps {
    script {
    while IFS=- read -r name version
do
    echo "$name" has made earnings of "$version" pounds today!
done < release.sh

    }

                }
            }
        }
    }

